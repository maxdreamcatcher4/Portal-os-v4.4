name = "portal-os-v1"
pages_build_output_dir = "dist"

# Pages-only configuration (UI/static site). This file intentionally excludes
# Worker, Durable Object, KV, and migrations so Cloudflare Pages validation
# treats this repository as a Pages project.
#
# If you need Pages environments, add an [env] table here. Worker runtime
# configuration lives in wrangler.worker.toml.
